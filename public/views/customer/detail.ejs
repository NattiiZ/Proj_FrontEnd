<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../css/style.css">
    <link rel="stylesheet" href="../../css/customer/detail.css">
    <link rel="shortcut icon" href="../img/logo/favico.png" type="image/x-icon">
    <title>รายละเอียดสินค้า</title>
</head>

<body>
    <header>
        <a href="/" class="logo"><img src="../img/logo/logo_store.png" alt="logo"></a>

        <form class="search-bar" action="/search" method="GET">
            <input type="text" name="query" placeholder="ค้นหาสินค้า..." />
            <button type="submit">ค้นหา</button>
        </form>

        <nav>
            <div><a href="/">หน้าแรก</a></div>
            <div class="dropdown">
                <a href="/all_category" class="dropbtn">หมวดหมู่</a>
                <div class="dropdown-content">
                    <% for(var i=0; i< category.length; i++) { %>
                        <a href="/category/<%=i+1%>"><%= category[i].name %></a>
                    <% } %>
                </div>
            </div>
            <div><a href="/cart">ตะกร้า</a></div>
            <% if (loginSession) { %>
                <div><a href="/account">ข้อมูลส่วนตัว</a></div>
            <% } else { %>
                <div><a href="/signin">เข้าสู่ระบบ</a></div>
            <% } %>
        </nav>
    </header>


    <div class="container">
        <h1 class="section-title">รายละเอียดสินค้า</h1>

        <div class="product-details">
            <div class="product-image">
                <img src="product-image.jpg" alt="Product Image" />
            </div>

            <div class="product-info">
                <% for(var i=0; i< products.length; i++) { %>
                    <% if (products[i].product_ID == Id) { %>
                        <h3><%= brand %> <%= products[i].name %></h3>
                        <% for(var k=0; k< category.length; k++) { %>
                            <% if (category[k].category_ID == products[i].category_ID) { %>
                                <p>ประเภท : <%= category[k].name %></p>
                            <% } %>
                        <% } %>
                        <b><p>ราคา <%= products[i].unitPrice %> บาท</p></b>

                        <h4>สเปคสินค้า:</h4>
                        <ul>
                            <li><strong>ชิปเซ็ต:</strong> NVIDIA GeForce RTX 4090</li>
                            <li><strong>หน่วยความจำ:</strong> 24GB GDDR6X</li>
                            <li><strong>ความเร็ว:</strong> 2.52 GHz</li>
                            <li><strong>เชื่อมต่อ:</strong> PCIe 4.0</li>
                            <li><strong>พอร์ตเชื่อมต่อ:</strong> 3x DisplayPort 1.4a, 1x HDMI 2.1</li>
                            <li><strong>รองรับการใช้งาน:</strong> 8K Gaming, Ray Tracing</li>
                            <li><strong>ระบบระบายความร้อน:</strong> 3 พัดลม, Vapor Chamber</li>
                        </ul>

                        <form action="getProduct" method="POST">
                            <button class="btn" type="submit">เพิ่มลงตะกร้า</button>
                        </form>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
</body>

<footer>
    &copy; 2025 DIY Computer | สงวนลิขสิทธิ์
</footer>

</html>