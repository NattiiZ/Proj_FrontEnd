<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../css/style.css">
    <link rel="stylesheet" href="../../css/customer/cart.css">
    <title>ตะกร้าสินค้า - DIY Computer</title>
</head>

<body>
    <header>
        <a href="/" class="logo"><img src="../../img/_logo_store.png" alt=""></a>

        <form class="search-bar" action="/search" method="GET">
            <input type="text" name="query" placeholder="ค้นหาสินค้า..." />
            <button type="submit">ค้นหา</button>
        </form>

        <nav>
            <div><a href="/">หน้าแรก</a></div>
            <div class="dropdown">
                <a href="/category" class="dropbtn">หมวดหมู่</a>
                <div class="dropdown-content">
                    <% for(var i=0; i< category.length; i++) { %>
                        <a href=""><%= category[i].name %></a>
                    <% } %>
                </div>
            </div>
            <div><a href="/cart">ตะกร้า</a></div>
            <div><a href="/account">ข้อมูลส่วนตัว</a></div>
        </nav>
    </header>
    </header>


    <div class="container">
        <h2 class="section-title">ตะกร้าสินค้าของคุณ</h2>
        
        <div class="cart-items">
            <div class="cart-item">
                <img src="path_to_image.jpg" alt="Product Image" class="cart-item-image">
                <div class="cart-item-info">
                    <h3>ASUS ROG Strix XG27AQ</h3>
                    <p>ราคา: 18,900 บาท</p>
                    <div class="quantity-control">
                        <button class="btn quantity-btn" onclick="decreaseQuantity()">-</button>
                        <span id="quantity">1</span>
                        <button class="btn quantity-btn" onclick="increaseQuantity()">+</button>
                    </div>
                    <a href="#" class="btn" onclick="alert('ลบสินค้าออกจากตะกร้า')">ลบ</a>
                </div>
            </div>
            <!-- Repeat the above block for other items -->
        </div>

        <div class="cart-summary">
            <p>รวมทั้งสิ้น: <span id="total-price">18,900</span> บาท</p>
            <a href="/checkout" class="btn">ชำระเงิน</a>
        </div>
    </div>

    <footer>
        &copy; 2025 DIY Computer | สงวนลิขสิทธิ์
    </footer>
</body>


<script>
    let quantity = 1;
    let price = 18900;

    function updateTotalPrice() {
        let totalPrice = quantity * price;
        document.getElementById("total-price").innerText = totalPrice.toLocaleString();
        document.getElementById("quantity").innerText = quantity;
    }

    function increaseQuantity() {
        quantity++;
        updateTotalPrice();
    }

    function decreaseQuantity() {
        if (quantity > 1) {
            quantity--;
            updateTotalPrice();
        }
    }
</script>


</html>
