<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/style.css">
    <title>ร้านขายอุปกรณ์คอม DIY</title>
</head>
<body>
    <header>
        <a href="#" class="logo">DIY Computer</a>
        <form class="search-bar" action="/search" method="GET">
            <input type="text" name="query" placeholder="Search..." />
            <button type="submit">Search</button>
        </form>
        <nav>
            <a href="#">หน้าแรก</a>
            <a href="#">หมวดหมู่</a>
            <a href="#">ตะกร้า</a>
            <a href="#">บัญชี</a>
        </nav>
    </header>

    <div class="banner"></div>

    <div class="container">
        <div class="product-grid">

            <% for(var i=0; i< products.length; i++) { %>

                <div class="product-card">
                    <img src="" alt="">
                    <div class="product-info">
                        <% for(var k=0; k< brands.length; k++) { %>
                            <% if (products[i].brand_ID == brands[k].brand_ID) { %>
                                <h3><%= brands[k].name %> <%= products[i].name %></h3>
                            <% } %>
                        <% } %>

                        <p>ราคา: <%= products[i].unitPrice %> บาท</p>
                        <a href="#" class="btn" onclick="alert('เพิ่ม <%= products[i].name %> ลงตะกร้า')">เพิ่มลงตะกร้า</a>
                        <a href="#" class="btn" onclick="alert('รายละเอียดสินค้า: <%= products[i].name %> - ราคา: <%= products[i].price %> บาท')">ดูรายละเอียด</a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <footer>
        &copy; 2025 DIY Computer | สงวนลิขสิทธิ์
    </footer>
</body>
</html>