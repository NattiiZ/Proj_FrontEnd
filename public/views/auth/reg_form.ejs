<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../css/style.css">
    <link rel="stylesheet" href="../css/signup.css">
    <link rel="shortcut icon" href="../img/favico.png" type="image/x-icon">
    <title>ข้อมูลผู้ใช้</title>
</head>

<style>
    .input-group {
        margin-bottom: 15px;
    }

    .input-group label {
        display: block;
        font-size: 14px;
        margin-bottom: 5px;
    }

    .input-group input,
    .input-group textarea {
        width: 96%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 14px;
    }

    .input-group textarea {
        resize: none; /* ปิดการขยายขนาด textarea */
    }

    .input-group input:focus,
    .input-group textarea:focus {
        border-color: #007bff;
        outline: none;
    }

    .input-group input::placeholder,
    .input-group textarea::placeholder {
        color: #aaa;
    }

    .btn {
        padding: 10px 20px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

    .btn:hover {
        background-color: #0056b3;
    }
</style>

<body>
    
    <header>
        <a href="/" class="logo"><img src="../img/logo_store.png" alt="logo"></a>

        <form class="search-bar" action="/search" method="GET">
            <input type="text" name="query" placeholder="ค้นหาสินค้า..." />
            <button type="submit">ค้นหา</button>
        </form>

        <nav>
            <div><a href="/">หน้าแรก</a></div>
            <div class="dropdown">
                <a href="/category" class="dropbtn">หมวดหมู่</a>
                <div class="dropdown-content">
                    <% for(var i=0; i< category.length; i++) { %>
                        <a href="/category/<%=category[i].category_name%>"><%= category[i].name %></a>
                    <% } %>
                </div>
            </div>
            <div><a href="/cart">ตะกร้า</a></div>
            <div><a href="/signin">เข้าสู่ระบบ</a></div>
        </nav>
    </header>


    <div class="container">
        <h1 class="section-title">ข้อมูลผู้ใช้</h1>

        <div class="register-form">
            <form action="/createUser" method="POST">
                <div class="input-group">
                    <label for="fullname">ชื่อ-นามสกุล</label>
                    <input type="text" id="fullname" name="name" required placeholder="กรอกชื่อ-นามสกุล"
                           pattern="[A-Za-zก-๙\s]{3,50}" title="กรุณากรอกชื่อ-นามสกุล (3-50 ตัวอักษร) โดยไม่มีอักขระพิเศษ" />
                </div>
            
                <div class="input-group">
                    <label for="phone">เบอร์โทร</label>
                    <input type="tel" id="phone" name="phone" required placeholder="กรอกเบอร์โทร"
                           pattern="0[0-9]{9}" title="กรุณากรอกเบอร์โทร 10 หลัก โดยขึ้นต้นด้วย 0" maxlength="10" />
                </div>
            
                <div class="input-group">
                    <label for="address">ที่อยู่</label>
                    <textarea id="address" name="address" required placeholder="กรอกที่อยู่" rows="3" maxlength="255"
                              pattern="[^ ]+" title="กรุณากรอกที่อยู่โดยไม่มีอักขระ space และไม่เกิน 255 ตัวอักษร"></textarea>
                </div>
            
                <button type="submit" class="btn">บันทึกข้อมูล</button>
            </form>
        </div>
        
    </div>

    
    <footer>
        &copy; 2025 The Computer Store | Copyright
    </footer>

</body>

</html>
